<?xml version="1.0" encoding="UTF-8"?>
<entities xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:noNamespaceSchemaLocation="http://moqui.org/xsd/entity-definition-3.xsd">

    <!-- Party Entity -->
    <entity entity-name="Party" package="party" table-name="PARTY_CUSTOM" title="Party">
        <field name="partyId" type="id" is-pk="true"/>
        <field name="partyTypeEnumId" type="id"/>
        <field name="createdDate" type="date-time"/>
    </entity>

    <!-- Person Entity -->
    <entity entity-name="Person" package="party" table-name="PERSON_CUSTOM" title="Person">
        <field name="partyId" type="id" is-pk="true"/>
        <field name="firstName" type="text-medium"/>
        <field name="lastName" type="text-medium"/>
        <field name="dateOfBirth" type="date"/>
        <relationship type="one" related="party.Party">
            <key-map field-name="partyId"/>
        </relationship>
    </entity>

    <!-- PartyGroup Entity -->
    <entity entity-name="PartyGroup" package="party" table-name="PARTY_GROUP_CUSTOM" title="Party Group">
        <field name="partyId" type="id" is-pk="true"/>
        <field name="groupName" type="text-medium"/>
        <field name="description" type="text-long"/>
        <relationship type="one" related="party.Party">
            <key-map field-name="partyId"/>
        </relationship>
    </entity>

    <!-- ContactMech Entity -->
    <entity entity-name="ContactMech" package="party" table-name="CONTACT_MECH_CUSTOM" title="Contact Mechanism">
        <field name="contactMechId" type="id" is-pk="true"/>
        <field name="contactMechTypeId" type="id"/>
        <field name="infoString" type="text-long"/>
        <field name="createdDate" type="date-time"/>
    </entity>

    <!-- PartyContactMech Entity -->
    <entity entity-name="PartyContactMech" package="party" table-name="PARTY_CONTACT_MECH_CUSTOM" title="Party Contact Mechanism">
        <field name="partyId" type="id" is-pk="true"/>
        <field name="contactMechId" type="id" is-pk="true"/>
        <field name="fromDate" type="date-time" is-pk="true"/>
        <field name="thruDate" type="date-time"/>
        <relationship type="one" related="party.Party">
            <key-map field-name="partyId"/>
        </relationship>
        <relationship type="one" related="party.ContactMech">
            <key-map field-name="contactMechId"/>
        </relationship>
    </entity>

</entities>
